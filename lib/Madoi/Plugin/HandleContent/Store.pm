package Madoi::Plugin::HandleContent::Store;
use strict;
use warnings;
use base qw(Madoi::Plugin);

sub register {
    my ($self, $context) = @_;
}

sub handle {
    my ($self, $dataref, $message) = @_;
}

1;
